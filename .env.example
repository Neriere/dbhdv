# Port and Host
PORT=3000
APP_HOST=0.0.0.0

# Optional Basic Auth (leave empty to disable)
APP_BASIC_AUTH_USER=
APP_BASIC_AUTH_PASSWORD=
APP_BASIC_AUTH_REALM="Acceso Privado DofusDB"

# Optional Remote Turso / LibSQL Database (defaults to file:local.db if omitted)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
